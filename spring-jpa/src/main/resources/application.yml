spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    data-password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      #更新或创建数据表，如果有就更新，如果没有就创建
      ddl-auto: update
      #控制台显示sql
    show-sql: true